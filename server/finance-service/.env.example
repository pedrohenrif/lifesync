NODE_ENV=development
PORT=4003
# MongoDB dedicado ao finance-service (database-per-service)
FINANCE_MONGODB_URI=mongodb://localhost:27020/lifesync_finance
# Mesma chave JWT compartilhada para validar tokens
JWT_SECRET=change-me-in-production
